# 6. 참가 인원수 제한하기

### 📅 날짜
2025-11-08

### 🔗 강의 링크
* [참가 인원수 제한하기 (링크)](https://www.youtube.com/watch?v=v7lCdps2WpY)

---

### 💡 핵심 요약
* 

### 🔑 주요 개념 및 코드

#### 1. `setContentView(R.layout.activity_main)`
모든 안드로이드 액티비티(화면)의 기본 설정입니다.  
`activity_main.xml`이라는 XML 레이아웃 파일(디자인 파일)을 가져와서 실제 화면에 그리라고 명령하는 함수입니다.  

#### 2. `enableEdgeToEdge()` 함수
이 함수를 호출하면, 앱의 화면(Activity)이 시스템 바(상태 표시줄, 내비게이션 바) 뒤쪽까지 확장됩니다.  
하지만 이 함수만 호출하면 앱의 UI가 시스템 바와 겹쳐 보이게 됩니다.  
따라서 이 함수는 `setOnApplyWindowInsetsListener`와 함께 사용되어야 합니다.

#### 3. `setOnApplyWindowInsetsListener`
```kotlin
{ v, insets -> // v: R.id.main 뷰, insets: 시스템 바 크기 정보
    // 1. 시스템 바(상단 상태표시줄, 하단 내비게이션 바)의 크기를 가져옵니다.
    val systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars())

    // 2. 가져온 크기만큼 뷰(v)의 '패딩(padding)'을 설정합니다.
    v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom)

    // 3. 처리가 끝났음을 알립니다.
    insets
}
```
